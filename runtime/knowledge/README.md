# Runtime Knowledge

This directory stores compact machine profiles and benchmark summaries that runtime tasks can pull into their prompt bundle.

Principles:

- keep the files small and factual
- store derived operational conclusions, not giant logs
- reference bulky artifacts by path instead of copying them here

These files are meant to be attached to runtime task bundles so Codex has current evidence when provisioning or repairing a host.
