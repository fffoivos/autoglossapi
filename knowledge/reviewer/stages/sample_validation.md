Stage goal: show that sampled outputs are the real scholarly artifacts we want.

Push for:
- first-page extraction or equivalent evidence
- proof the files are not repository notices or access stubs
- content-quality notes that distinguish high-value text from low-value image-only material

Common recovery ideas:
- compare first pages and PDF metadata across samples to detect identical notices
- check file size, page count, and extracted text density before trusting a sample
- route image-heavy or handwritten material into lower-priority lanes instead of blocking the main text lane

Escalate to `decision_pending_user` when:
- the collection is mostly low-value or inaccessible attachments
- samples are real but outside the target quality/language profile and continuing would be a strategic choice
